
```
apt-get install php-cli php-curl php-zip
git clone...
git submodule update --init
compoesr.phar install
crontab -e #*/5 * * * * php /path/to/dd-php-simple-srcds-metrics/src/main.php > dev/null
```

